var AppConfig = {
			api_server: { // local testing instance
				host: "localhost",
				port: 3000
			}
			/*
			,
			api_server: { // instance deployed on AppFog
				host: "oskhelper.eu01.aws.af.cm",
				port: 80
			}
			*/
		}